package com.code.practice.example.practice1;

public interface Car {
	
	 String changeGear(String movement);
	 String changedirection(String direction);
	 float accelerator(); // 
	 boolean horn();
	 String headlight(); 
	 String indicator(); 

}
